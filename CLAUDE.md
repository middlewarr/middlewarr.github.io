# CLAUDE.md

Read [AGENTS.md](AGENTS.md) first — it is the single source of truth for this repo's commands and
constraints, in particular the fact that `pnpm test` requires a fresh `pnpm build`.

Keep project knowledge in AGENTS.md so every agent sees it. This file is only for
Claude-specific configuration, and currently has none.
